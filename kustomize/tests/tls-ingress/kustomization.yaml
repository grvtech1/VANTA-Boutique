# CI render test: Ingress + cert-manager TLS component (patch ordering must stay valid).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../../base
components:
  - ../../components/ingress
  - ../../components/tls
