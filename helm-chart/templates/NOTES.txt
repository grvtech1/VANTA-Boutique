VANTA Boutique deployed to namespace {{ .Release.Namespace }}.

Watch it come up:
    kubectl get pods -n {{ .Release.Namespace }} -w

{{- if and .Values.frontend.create .Values.frontend.externalService }}

Frontend (LoadBalancer — may take a minute to get an address):
    kubectl get svc -n {{ .Release.Namespace }} -w {{ .Values.frontend.name }}-external
{{- end }}

Or port-forward:
    kubectl port-forward -n {{ .Release.Namespace }} svc/{{ .Values.frontend.name }} 8080:80   → http://localhost:8080

{{- if .Values.reviewsService.create }}
Reviews service: {{ .Values.reviewsService.replicas }} replica(s), store = {{ ternary "PostgreSQL" "in-memory" .Values.reviewsService.database.enabled }}.
{{- end }}
