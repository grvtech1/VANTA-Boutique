# Chart derived from the upstream Online Boutique chart (Apache-2.0, Google LLC); adapted for
# VANTA Boutique: reviewsservice template, per-service image overrides, cloud-neutral telemetry.
apiVersion: v2
name: vanta-boutique
description: VANTA Boutique — polyglot gRPC microservices storefront with a Reviews service
type: application
version: 1.0.0
# Default tag for the unmodified upstream services; this repo's own images use images.overrides.
appVersion: "v0.10.5"
keywords: [microservices, grpc, kubernetes, gitops]
sources:
  - https://github.com/grvtech1/VANTA-Boutique
  - https://github.com/GoogleCloudPlatform/microservices-demo
